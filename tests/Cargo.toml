[package]
name = "tests"
version = "0.1.0"
edition = "2024"

[dependencies]
kernel = { path = "../src/kernel" }

[lib]
path = "src/lib.rs"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

